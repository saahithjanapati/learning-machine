# canvas-07-01-13085977-Chapter2.RVs

Source: `materials/archive/probability-and-statistics-36-700-cmu/canvas-modules/canvas-07-01-13085977-Chapter2.RVs.pdf`
Duplicate equivalents: `canvas-07-01-13085977-Chapter2.RVs.pdf`
Extraction engine: `Gemini API (gemini-3-flash-preview)`
Strategy: `chunked inline-PDF conversion`
Finish reason: `STOP`
Pages: 13

## Page 1
### Content
# 36700 CHAPTER 2: Random Variables and Distributions

### Contents
1. **Random Variables** (1)
2. **Distribution Functions** (2)
3. **Density Functions and Mass Functions** (5)
4. **Some Famous Univariate Distributions** (5)
    4.1 Continuous Distributions (6)
    4.2 Discrete Distributions (6)
5. **Multivariate Distributions** (7)
    5.1 Marginal and Conditional Distributions (8)
    5.2 Independence (8)
6. **Calculating Probabilities of Events** (9)
7. **Transformations of Random Variables** (10)
    7.1 Discrete Case (10)
    7.2 Continuous Case (11)

Reading: All of Statistics (AOS) Sec 2.1-2.12.

## 1 Random Variables
Often we are interested in dealing with summaries of experiments rather than the actual outcome. For instance, suppose I we flip a coin 100 times. Then $|\Omega| = 2^{100}$. But we may only be interested in a summary such as the number of heads. These summary statistics are called random variables.

**Definition:** A random variable is a function from the sample space $\Omega$ to the reals.

One way of thinking about a random variable is as a mapping between a distribution on $\Omega$ to a distribution on the reals (i.e. the range of the random variable). Formally, we have that for some subset $A \subset \mathbb{R}$,
$$\mathbb{P}_X(X \in A) = \mathbb{P}(\{\omega \in \Omega : X(\omega) \in A\}).$$
$\mathbb{P}_X$ is usually called the induced probability distribution.

**Revisit Example Chapter 1 section 1.4:** There are two black balls and three white balls in a bag. Two balls are randomly drawn, without replacement, from the bag. What is the probability that the two balls have different colors?

### Visual Description
Text-only slide.

---
## Page 2
### Content
We could define a random variable $X = \text{number of black balls drawn}$ and calculate the required probability as $\mathbb{P}(X = 1)$. $X$ can take value 0, 1, or 2 with probabilities:

$\mathbb{P}_X(X = 0) = \mathbb{P}(\{\omega = WW\}) = 6/20$
$\mathbb{P}_X(X = 1) = \mathbb{P}(\{(\omega = WB) \cup (\omega = BW)\}) = 6/20 + 6/20$
$\mathbb{P}_X(X = 2) = \mathbb{P}(\{\omega = BB\}) = 2/20$
$\square$

**Example:** Toss a fair coin three times. Let $X$ be the number of heads.
The sample space of the experiment is $\Omega = \{HHH, HHT, HTH, THH, TTH, THT, HTT, TTT\}$. All these outcomes are equally likely and disjoint. Then
$$\mathbb{P}_X(X = 0) = \frac{1}{8} \quad \mathbb{P}_X(X = 1) = \frac{3}{8}$$
$$\mathbb{P}_X(X = 2) = \frac{3}{8} \quad \mathbb{P}_X(X = 3) = \frac{1}{8}.$$
$\square$

There are several cases when we can write down $\mathbb{P}_X$ directly, often when it is too unwieldy to write down $\mathbb{P}$ and then compute the induced $\mathbb{P}_X$.

**Example:** Suppose we toss a coin with $\mathbb{P}(\text{heads}) = p$, and $\mathbb{P}(\text{tails}) = 1 - p$. The outcome of any particular toss has what we call a Bernoulli distribution. The number of heads in $n$ tosses is a random variable which has an induced distribution:
$$\mathbb{P}_X(X = k) = \binom{n}{k} p^k (1 - p)^{n-k}.$$
This is known as the binomial distribution. $\square$

## 2 Distribution Functions
Every random variable is associated with a cumulative distribution function (CDF).

**Definition:** The CDF of a random variable is:
$$F_X(x) = \mathbb{P}_X(X \le x), \quad \forall x.$$
What is the point $x$ for which $F_X(x) = 0.5$ called? More generally, these are known as quantiles.

### Visual Description
Text-only slide.

---
## Page 3
### Content
**Example Chapter 1 section 1.4:** Let $X = \text{number of black balls}$. Then, for:
$x < 0, \quad F_X(x) = 0$
$x \in [0, 1), \quad F_X(x) = 6/20$
$x \in [1, 2), \quad F_X(x) = 6/20 + 12/20$
$x \ge 2, \quad F_X(x) = 1.$
Draw this CDF.
$\square$

**Example:** Suppose we toss a coin repeatedly until we see a head, and let $X$ be the number of tosses. Then
$$\mathbb{P}_X(X = x) = (1 - p)^{x-1}p \quad \forall x = 1, 2, \dots,$$
We can see that for any positive integer $x$:
$$F_X(x) = \mathbb{P}_X(X \le x) = \sum_{i=1}^x (1 - p)^{i-1}p$$
$$= \frac{1 - (1 - p)^x}{1 - (1 - p)}p = 1 - (1 - p)^x.$$
What does this CDF look like?
$\square$

We say several examples of distributions we could derive. More generally, any function $F$ is a CDF if and only if:
1. $\lim_{x \to -\infty} F(x) = 0$, and $\lim_{x \to \infty} F(x) = 1$.
2. It is a non-decreasing function of $x$.
3. The CDF is right-continuous, i.e. for every number $x_0$
$$\lim_{x \to x_0^+} F(x) = F(x_0).$$
The CDF of any random variable will satisfy these conditions. Check the examples above. Conversely, if $F$ satisfies these three conditions then there exists a random variable with this distribution.

**Example:** Suppose that for any real number $x$,
$$F_X(x) = \frac{1}{1 + \exp(-x)}.$$
Is this a valid CDF? We need to verify the three conditions.

### Visual Description
Text-only slide.

---
## Page 4
### Content
1. Since, $\exp(-x)$ tends to $\infty$ as $x \to -\infty$ and $0$ as $x \to \infty$, it is clear that the first property holds.
2. $\exp(-x)$ is a decreasing function of $x$, so $1 + \exp(-x)$ is a decreasing function of $x$ and so $F_X(x) = \frac{1}{1+\exp(-x)}$ is an increasing function of $x$.
You could also show that the first derivative of $F_X(x)$ is positive:
$$\frac{d}{dx}F_X(x) = \frac{\exp(-x)}{(1 + \exp(-x))^2} > 0,$$
3. Since, it is differentiable it is clear that the distribution function is continuous not just right-continuous. $\square$

**Continuous and discrete random variables** We say that $X$ is a continuous random variable if its CDF $F_X(x)$ is a continuous function of $x$, and analogously it is discrete if its CDF $F_X(x)$ is a step function of $x$, i.e., it can be written as a finite linear combination of indicators of intervals.

For a continuous random variable $X$, the probability that $X = x$ is 0 for every $x$. There are many ways to think about this, but here is the mathematically rigorous way: note that $\{X = x\} \subset \{x - \epsilon < X \le x\}$ for any $\epsilon > 0$, so that
$$\mathbb{P}(X = x) \le \mathbb{P}(x - \epsilon < X \le x) = F_X(x) - F_X(x - \epsilon).$$
Now we just note that the RHS tends to 0 since the CDF of a continuous RV is continuous.

**Identically distributed random variables** This is an important concept. Two random variables $X$ and $Y$ are identically distributed if for any (measurable) set $A$,
$$\mathbb{P}_X(X \in A) = \mathbb{P}_Y(Y \in A). \quad (1)$$
Identically distributed does not mean equal. Toss a fair coin $n$ times, where $n$ is even, and let $X$ be the number of heads and $Y$ be the number of tails. These are identically distributed random variables but are clearly unequal since $Y = n - X$.

Important result: the following two statements are equivalent.
1. The random variables $X$ and $Y$ are identically distributed.
2. Their distribution functions are equal, i.e. $F_X(x) = F_Y(x)$ for all $x$.

It is easy to check that if (1) holds then (2) holds since we can just use eq.(1) with $A = (-\infty, x]$. Proving that (2) implies (1) is substantially more involved.

### Visual Description
Text-only slide.

---
## Page 5
### Content
## 3 Density Functions and Mass Functions
First a note about notation: we always use upper case letters $F_X(x)$ for CDFs and lower case letters $f_X(x)$ for density/mass functions.

For a discrete random variable, we associate a probability mass function, which is given by:
$$f_X(x) = \mathbb{P}_X(X = x).$$
For a continuous random variable, this definition does not really make sense since the probability that $X = x$ is 0 for every $x$. Instead we define the probability density function $f_X$ as the function that satisfies:
$$F_X(x) = \mathbb{P}_X(X \le x) = \int_{-\infty}^x f_X(t)dt \quad \forall x.$$
We often use the symbol $\sim$ to denote "distributed as", i.e. $X \sim \text{Ber}(p)$ means that the random variable $X$ has a Bernoulli distribution with parameter $p$.

A consequence of this definition and the fundamental theorem of calculus is that we can always calculate the pdf from the CDF as:
$$f_X(x) = F'_X(x).$$
This is sometimes used as the definition of the density function.

Note that if we have a pdf/pmf, integrating it to obtain the CDF does not always yield a closed form expression, e.g. the Normal distribution.

## 4 Some Famous Univariate Distributions
Just as with CDFs, there is a one-to-one correspondence between density/mass functions and functions that satisfy some basic properties: any function $f_X(x)$ is a pdf/pmf if and only if:
1. $f_X(x) \ge 0$ for all $x$.
2. $\sum_x f_X(x) = 1$ (pmf) in the discrete case or $\int_{-\infty}^\infty f_X(x)dx = 1$ (pdf) in the continuous case.

Because particular density/mass functions keep popping up in the probability and statistics fields, they were given names to make it easier to refer to them.

### Visual Description
Text-only slide.

---
## Page 6
### Content
### 4.1 Continuous Distributions
**Continuous Uniform Distribution:** On $[a, b]$ has pdf:
$$p_X(x) = \frac{1}{b - a} \mathbb{I}(x \in [a, b]).$$
We will use the notation $U[a, b]$.
**Exercise:** what is the CDF? $\square$

**Gaussian Distribution:** This is the classic bell-curve. It has a location (mean) and scale (standard deviation) parameter, usually denoted as $\mu$ and $\sigma$. It has pdf
$$p_X(x) = \frac{1}{\sqrt{2\pi}\sigma} \exp \left( -\frac{(x - \mu)^2}{2\sigma^2} \right).$$
We will use the notation $N(\mu, \sigma^2)$. A standard Normal distribution has $\mu = 0, \sigma = 1$. We will see this distribution many times throughout this course.

We use $\Phi(x)$ to denote the CDF of a standard Normal RV. Note that it cannot be obtained in closed form (the integral of the PDF is intractable).

The Normal distribution is part of the "location-scale" family of distribution. Basically, if $X \sim N(\mu, \sigma^2)$, then the shifted and rescaled RV $Z = (X - \mu)/\sigma$ is standard Normal. Conversely, if $Z$ is a standard normal RV, then $\forall(a, b), X = a + bZ \sim N(a, b^2)$

### 4.2 Discrete Distributions
**Discrete Uniform Distribution:** On $k$ categories $\{x_1, x_2, \dots, x_k\}$ the distribution
$$p_X(x) = \frac{1}{k} \quad \text{if } x \in \{x_1, \dots, x_k\},$$
is the discrete uniform distribution on $\{x_1, x_2, \dots, x_k\}$.

**The Bernoulli Distribution:** We have seen this one before: this is the distribution of a coin toss when the coin has bias $p$, we use 1 to denote heads and 0 to denote tails. The Bernoulli pmf is:
$$p_X(x) = p^x(1 - p)^{1-x}$$
for $x \in \{0, 1\}$. We will use the notation $\text{Ber}(p)$.

**The Binomial Distribution:** This is the distribution of the number of heads in $n$ tosses:
$$p_X(x) = \binom{n}{x} p^x(1 - p)^{n-x} \mathbb{I}(x \in \{0, 1, \dots, n\}).$$

### Visual Description
Text-only slide.

---
## Page 7
### Content
We will use the notation $\text{Bin}(n, p)$. Note that $\text{Ber}(p) = \text{Bin}(1, p)$.
Note that the CDF cannot be obtained in closed form (the integral of the PDF is intractable).

**The Geometric Distribution:** This is the distribution of the number of tosses to see 1 head. It has pmf:
$$p_X(x) = p(1 - p)^{x-1} \quad x \in \{1, 2, \dots\}.$$
We will use the notation $\text{Geom}(p)$.
The CDF was derived earlier in the notes.

**The Poisson Distribution:** The Poisson distribution often arises in modelling various types of arrival processes. If we have independent arrivals (buses arriving at a bus stop), a fixed rate of arrivals, and if we have that the number of arrivals in any interval of time is proportional to the length of the interval, then the number of arrivals in an interval will have a Poisson distribution.
The Poisson distribution is discrete with pmf:
$$\mathbb{P}(X = k) = \exp(-\lambda) \frac{\lambda^k}{k!} \quad x \in \{0, 1, \dots\}.$$
We will use the notation $\text{Poi}(\lambda)$.
Note that the CDF cannot be obtained in closed form (the integral of the PDF is intractable).

## 5 Multivariate Distributions
Suppose we have a pair of discrete random variables $X, Y$. We can define their **joint** pmf by:
$$f_{XY}(x, y) = \mathbb{P}(X = x \cup Y = y) = \mathbb{P}(X = x \text{ and } Y = y) = \mathbb{P}(X = x, Y = y).$$
In the continuous case the joint density function is the non-negative function $f_{XY}$ that integrates to give us probabilities, i.e.
$$\iint_{(x,y) \in S} f_{XY}(x, y) dx dy = \mathbb{P}((X, Y) \in S)$$
for any set $S \subset \mathbb{R} \times \mathbb{R}$. This definition extends to more than two RVs.

**Example:** Suppose $(X, Y)$ are jointly uniform over the unit square. Then it has density:
$$f_{XY}(x, y) = 1,$$

### Visual Description
Text-only slide.

---
## Page 8
### Content
if $0 \le x \le 1$ and $0 \le y \le 1$; otherwise $f_{XY}(x, y) = 0$. To calculate the probability $\mathbb{P}(X \le 1/4, Y \le 1/2)$, we integrate the joint density:
$$\mathbb{P}(X \le 1/4, Y \le 1/2) = \int_0^{1/4} \int_0^{1/2} 1 dx dy = \frac{1}{8}.$$
$\square$

**Exercise:** The multivariate Normal distribution arises frequently. Check a book for the pdf formula. Draw the bivariate case. $\square$

### 5.1 Marginal and Conditional Distributions
We can obtain the pmf/pdf of $X$ by summing/integrating the joint pmf/pdf of $(X, Y)$:
$$f_X(x) = \int f_{XY}(x, y) dy,$$
in the continuous case, and similarly for $Y$. For discrete RVs, replace the integral sign by a summation sign. For continuous RVs, the conditional distribution of $X$ given $Y$ can be obtained as:
$$f_{X|Y}(x | y) = f_{XY}(x, y)/f_Y(y).$$
For discrete RVs,
$$\mathbb{P}(Y = y|X = x) = p(y|x) = \frac{p(x, y)}{p(x)} = \frac{\mathbb{P}(Y = y, X = x)}{\mathbb{P}(X = x)}.$$
This generalizes to more than two RVs.

**Exercise:** Illustrate marginals and conditional distributions using the bivariate Normal distribution. $\square$

### 5.2 Independence
$X$ and $Y$ are independent if for every pair of sets $A, B$ we have that:
$$\mathbb{P}(X \in A, Y \in B) = \mathbb{P}(X \in A)\mathbb{P}(Y \in B).$$
In words, we say that two random variables are independent if their joint probability is equal to the product of their marginal probabilities. This seems to suggest that we need to check

### Visual Description
Text-only slide.
## Page 9
### Content
this condition for every possible pair of sets $A$ and $B$. It turns out that we can instead check that the joint pmf/pdf factorizes, i.e., that
$$f_{XY}(x, y) = f_X(x)f_Y(y),$$
for all $(x, y)$. If you can write the joint probability of two random variables as the product of a function of just the first one, and just the second one then the two random variables are independent, i.e., if
$$f_{XY}(x, y) = h(x)g(y),$$
for some pair of functions $h, g$ then the two random variables are independent.

**Example:** Suppose $X$ and $Y$ have density:
$$f_{XY}(x, y) = 2 \exp(-(x + 2y)),$$
for $x \ge 0, y \ge 0$. Are $X$ and $Y$ independent?
We see that we can write their joint density as:
$$f_{XY}(x, y) = 2 \exp(-(x + 2y)) = 2 \exp(-x) \exp(-2y) = h(x)g(y),$$
so we can conclude that these random variables are independent. $\square$

More generally, we can define joint independence for a set of RVs $X_1, \dots, X_n$ by requiring that their joint pmf/pdf factorizes as:
$$f_{X_1X_2...X_n}(x_1, x_2, \dots, x_n) = \prod_{i=1}^n f_{X_i}(x_i),$$
or as the product of $n$ functions of a single variable $x_i$.

Finally, if you are told that a set of RVs $X_1, \dots, X_n$ are mutually independent, each with mass/density function $f_{X_i}(x_i)$, then the joint mass/density function of $X_1, \dots, X_n$ is
$$f_{X_1X_2...X_n}(x_1, x_2, \dots, x_n) = \prod_{i=1}^n f_{X_i}(x_i).$$

### 6 Calculating Probabilities of Events
We can calculate the probability that $X$ is in some set $\mathcal{S}$ as:
$$\mathbb{P}(X \in \mathcal{S}) = \int_{x \in \mathcal{S}} f_X(x)dx \quad \text{or} \quad \sum_{x \in \mathcal{S}} f_X(x)$$

9

### Visual Description
Text-only slide.

---
## Page 10
### Content
for continuous and discrete random variables, respectively. For example, if $\mathcal{S}$ is the interval $[a, b]$ then,
$$\mathbb{P}(a < X \le b) = \int_a^b f_X(x)dx \quad \text{or} \quad \sum_{x>a}^{x=b} \mathbb{P}(X = x)$$
for continuous and discrete random variables, respectively, where the sum runs over the points $x$ for which $\mathbb{P}(X = x)$ is non-zero. We can also use the CDF to obtain the required probability:
$$\mathbb{P}(a < X \le b) = F_X(b) - F_X(a),$$
which is valid for both discrete and continuous random variables.

This extends to multivariate random variables. For example, in the continuous bivariate case,
$$\mathbb{P}((X, Y) \in \mathcal{S}) = \iint_{(x,y) \in \mathcal{S}} f_{XY}(x, y) dx dy$$
for any set $\mathcal{S} \subset \mathbb{R} \times \mathbb{R}$. The double integral would be replaced by a double summation in the discrete case.

If the required probability involves $n$ random variables, then you need to integrate or sum over a joint pmf/pdf, which involves $n$ integrations or summations.

**Example:** See various exercises in the next sections.

### 7 Transformations of Random Variables
Suppose $X$ has pdf/pmf $f_X$ and CDF $F_X$. Let $Y = r(X)$, for some function $r$. For instance, $r(X)$ might be something like $X^2$ or $\exp(X)$. How do I compute the pdf/pmf or CDF of $Y$? The first thing you should figure out is whether $Y$ is discrete or continuous, because methods are different for one and the other, and what values it can take.

#### 7.1 Discrete Case
If $X$ is discrete and $Y$ is discrete, then the pmf of $Y$ is given by:
$$\mathbb{P}(Y = y) = \mathbb{P}(r(X) = y)$$
$$= \mathbb{P}(\{x : r(x) = y\}) = \mathbb{P}(X \in r^{-1}(y)).$$

10

### Visual Description
Text-only slide.

---
## Page 11
### Content
**Example:** suppose $X \in \{-1, 0, 1\}$, with probabilities $1/4, 1/2$ and $1/4$, and consider the random variable $Y = X^2$. First, we determine that $Y$ is discrete and can take only two values, $\{0, 1\}$. We therefore need to calculate $\mathbb{P}(Y = 0)$ and $\mathbb{P}(Y = 1)$ to know the distribution of $Y$.
$$\mathbb{P}(Y = 0) = \mathbb{P}(X^2 = 0) = \mathbb{P}(X = 0) = 1/2$$
$$\mathbb{P}(Y = 1) = \mathbb{P}(X^2 = 1) = \mathbb{P}((X = 1) \cup (X = -1)) = 1/4 + 1/4 = 1/2$$
$\square$

You can also have the case $X$ continuous and $Y$ discrete, for example $X$ is standard Normal, and $Y$ is defined as $Y = 0$ if $X < 0$, and $Y = 1$ if $X \ge 0$. Again, all you need to do is calculate $\mathbb{P}(Y = y)$ for all the possible values $y$ that $Y$ can take, and express $\mathbb{P}(Y = y)$ in terms of $X$. Here,
$$\mathbb{P}(Y = 0) = \mathbb{P}(X < 0) = 1/2$$
$$\mathbb{P}(Y = 1) = \mathbb{P}(X \ge 0) = 1/2.$$

#### 7.2 Continuous Case
If $Y$ is continuous, then $X$ is continuous as well. Transformations for continuous RVs are substantially more involved, and there are several methods. I will sketch the basic ideas here.

**The CDF method** It is the easiest method but the result is not always tractable. The jacobian method, below, is more general but not always as easy to apply.

Once you have determined that $Y$ is continuous with values $y$ in some set, calculate its CDF $F_Y(y) = \mathbb{P}(Y \le y)$ for all $y$ in that set, by expressing $\mathbb{P}(Y \le y)$ in terms of $X$ and using the distribution of $X$ to complete the derivation, i.e.:
$$\mathbb{P}(Y \le y) = \mathbb{P}(r(X) \le y)$$
$$= \mathbb{P}(\{x : r(x) \le y\}) = \mathbb{P}(X \in r^{-1}(y)),$$
then differentiate $F_Y(y)$ wrt $y$ to obtain the pdf of $Y$. i.e. $f_Y(y) = F'_Y(y)$.

**Example 1:** Let $X = U[-1, 1]$ and $Y = X^2$. $Y$ is continuous with values in $[0, 1]$. Then for $y \le 0, F_Y(y) = 0$ and $f_Y(y) = 0$, for $y \ge 1, F_Y(y) = 1$ and $f_Y(y) = 0$, and for $y \in [0, 1]$,
$$\mathbb{P}(Y \le y) = \mathbb{P}(X^2 \le y)$$
$$= \mathbb{P}(X \in [-\sqrt{y}, \sqrt{y}]) = \int_{-\sqrt{y}}^{\sqrt{y}} 1/2 dx.$$

11

### Visual Description
Text-only slide.

---
## Page 12
### Content
Since we want the pdf, there is not need calculate the integral (in this case it is possible and easy, but that is not always the case). Just take the derivative wrt $y$ to obtain $f_Y(y) = 1/(2\sqrt{y})$ for $y \in [0, 1]$, and $f_Y(y) = 0$ otherwise. $\square$

**Example 2:** The Cauchy distribution is the distribution of the ratio of two independent standard Gaussian random variables, i.e., if I draw $X_1 \sim N(0, 1)$ and $X_2 \sim N(0, 1)$ independently and compute $Y = X_1/X_2$ it will have a Cauchy distribution, with pdf:
$$f_Y(y) = \frac{1}{\pi(1 + y^2)}.$$
Let's prove this (to practice transformations and probability calculations).

First, we determine that $Y = r(X_1, X_2) = X_1/X_2$ is continuous with values in $(-\infty, +\infty)$. So we need to calculate the PDF for all $y$:
$$F_Y(y) = \mathbb{P}(Y \le y) = \mathbb{P}(r(X_1, X_2) \le y) = \iint_{A_y} f(x_1, x_2) dx_1 dx_2$$
where $A_y = \{(x_1, x_2) : r(x_1, x_2) \le y\}$. $\square$

**Exercise:** Let $X \sim U[0, 1]$ and $Y = X^2$. Find the pdf of $Y$ using the CDF method. $\square$

**The Jacobian method** There is one case when things simplify: **suppose that the transformation $r$ is invertible**, $s = r^{-1}$, with $s$ differentiable, then we have the formula:
$$f_Y(y) = f_X(s(y)) \left| \frac{ds(y)}{dy} \right|,$$
where the last term is the determinant of the matrix of the derivatives of the old variables with respect to the new variables. This formula is valid for univariate and multivariate random variables.

**Example:** consider $X \sim U[0, 1]$ and $Y = X^2$ (note that I did not consider $X = U[-1, 1]$ because then the transformation would not be invertible). $Y$ is continuous and takes values in $[0, 1]$. Before we apply the formula, we should try to guess what we think the pdf of $Y$ would be. Squaring a number between $[0, 1]$ only makes it smaller, so we should expect that the density of $Y$ would be peaked near 0, i.e., the uniform density will get "squeezed" towards 0.

Now, we can apply the formula. Observe that $r(X) = X^2$, and since $X \ge 0, s(Y) = \sqrt{Y}$, so we have that:
$$f_Y(y) = \frac{1}{2\sqrt{y}}.$$

12

### Visual Description
Text-only slide.

---
## Page 13
### Content
Graphing this density will show that it matches our intuition. Notice again, a density is not a probability! This density $\to \infty$ near 0. $\square$

**Some intuition for the Jacobian formula:**
In general, dealing with densities takes some getting used to. Roughly, for a continuous random variable one should think of multiplying the density by the length of a small interval to approximate the probability of the random variable falling in this interval.

When you transform the random variable then the transformation affects *both* the value of the density and also the length of the interval. The term $\left| \frac{ds(y)}{dy} \right|$, is called the Jacobian of the transformation and accounts for the stretching of the interval caused by the transformation.

Here is a very rough mathematical calculation. For a very small $\Delta$:
$$\mathbb{P}(Y \in [y_0 - \Delta, y_0 + \Delta]) \approx f_Y(y_0)2\Delta.$$
If $Y$ falls in the interval this is the same as saying that $X$ falls in a slightly different interval, i.e.,
$$\mathbb{P}(Y \in [y_0 - \Delta, y_0 + \Delta]) = \mathbb{P}(X \in [s(y_0 - \Delta), s(y_0 + \Delta)])$$
$$\approx \mathbb{P} \left( X \in \left[ s(y_0) - \Delta \frac{ds(y_0)}{dy}, s(y_0) + \Delta \frac{ds(y_0)}{dy} \right] \right)$$
$$\approx f_X(s(y_0))2\Delta \left| \frac{ds(y_0)}{dy} \right|,$$
where in the second line we assumed the derivative was positive (otherwise we would flip the end points of the interval). Equating these gives the result.

**Non-invertible case:** In that case, you need to split the space in parts where the transformation is invertible and sum up the bits from each sub-space. See C&B.

**$s(y)$ not differentiable:** Use the CDF method or the **infinitesimal method**. I will not cover this here (it is similar to the rough mathematical calculation above), but it is very useful in some cases, e.g. to obtain the distribution of order statistics.

**Exercise:** If I draw $X_1 \sim N(0, 1)$ and $X_2 \sim N(0, 1)$ independently and compute $Y = X_1/X_2$, it will have a Cauchy distribution. Derive the result using the Jacobian method.
(First, determine that $Y$ is continuous with values in $(-\infty, +\infty)$. Next determine that the transformation is differentiable and 1-1 (invertible). )

13

### Visual Description
Text-only slide.

---
