# canvas-12-07-13301500-Chapter7.Estimation

Source: `materials/archive/probability-and-statistics-36-700-cmu/canvas-modules/canvas-12-07-13301500-Chapter7.Estimation.pdf`
Duplicate equivalents: `canvas-12-07-13301500-Chapter7.Estimation.pdf`
Extraction engine: `Gemini API (gemini-3-flash-preview)`
Strategy: `chunked inline-PDF conversion`
Finish reason: `STOP`
Pages: 18

## Page 1
### Content
# CHAPTER 7 – Point Estimation

### Contents
1. **Finding Estimators** (1)
   1.1 Plug-In Estimators (2)
   1.2 Least Square Estimators (2)
   1.3 Method of Moments Estimators (2)
   1.4 Maximum Likelihood Estimators (4)
   1.5 Bayes Estimators (6)
2. **Properties of MLEs** (8)
   2.1 Consistency (8)
   2.2 Fisher Information (8)
   2.3 Asymptotic Normality of the MLE (10)
   2.4 Multiparameter Problems (13)
3. **Evaluating/Comparing Estimators** (13)
   3.1 Decision Theory (13)
   3.2 Admissibility (14)
   3.3 Bayes Estimator (15)
   3.4 Minimax Estimators (16)
   3.5 Connections (17)

Reference: Wasserman Sec. 9.1-9.10, Sec 11.1-11.3, Ch. 12

## 1 Finding Estimators
Point estimation is aligned with the general statistical pursuit of trying to understand an underlying population from a sample. Particularly, under the hypothesis that the sample was generated from some parametric statistical model, a natural way to understand the underlying population is by estimating the parameters of the statistical model.

One can of course wonder what happens if the model is wrong? Or where do models really come from? In some rare cases, we actually know enough about our data to hypothesise a reasonable model. Most often however, when we specify a model, we do so hoping that it can provide a useful approximation to the data generation mechanism. The George Box quote is worth remembering in this context: “all models are wrong, but some are useful.”

### Visual Description
Text-only slide.

---
## Page 2
### Content
### 1.1 Plug-In Estimators
### 1.2 Least Square Estimators
### 1.3 Method of Moments Estimators

Suppose that $\theta = (\theta_1, \dots, \theta_k)$ so that there are $k$ unknown parameters; e.g. $\theta = (\mu, \sigma^2)$ for the Gaussian distribution. We can estimate $\theta$ by matching $k$ theoretical/population moments and $k$ sample moments.

Let
$$m_1 = \frac{1}{n} \sum_{i=1}^n X_i, \quad m_2 = \frac{1}{n} \sum_{i=1}^n X_i^2, \quad \dots, \quad m_k = \frac{1}{n} \sum_{i=1}^n X_i^k,$$
be the first $k$ sample moments and
$$\mu_i = \int x^i f_\theta(x) dx, \quad i = 1, \dots, k$$
be the first $k$ theoretical moments. The $\mu_i$’s are functions of the unknown parameters, so we write them as $\mu_i(\theta)$.

The method-of-moments prescribes estimating the parameters: $\theta_1, \dots, \theta_k$ by solving the system of equations:
$$m_1 = \mu_1(\theta_1, \dots, \theta_k)$$
$$\vdots$$
$$m_k = \mu_k(\theta_1, \dots, \theta_k).$$

**Example 1:** If $X_1, \dots, X_n \sim N(\theta, \sigma^2)$ (note that we have to estimate 2 parameters so we need to use 2 moments), we solve:
$$\frac{1}{n} \sum_{i=1}^n X_i = \theta, \quad \text{and} \quad \frac{1}{n} \sum_{i=1}^n X_i^2 = \theta^2 + \sigma^2,$$

### Visual Description
Text-only slide.

---
## Page 3
### Content
to obtain the estimators:
$$\hat{\theta} = \frac{1}{n} \sum_{i=1}^n X_i = \bar{X}_n$$
$$\hat{\sigma}^2 = \frac{1}{n} \sum_{i=1}^n X_i^2 - \left( \frac{1}{n} \sum_{i=1}^n X_i \right)^2 = \frac{1}{n} \sum_{i=1}^n (X_i - \bar{X}_n)^2 \equiv s_n^2,$$
which are natural estimators of these quantities. You can see that the method-of-moments estimators can be biased since in this example the estimator for the variance has non-zero bias.

**Example 2:** Suppose $X_1, \dots, X_n \sim \text{Bin}(k, p)$, i.e. each $X_i$ is the sum of $k$ $\text{Ber}(p)$ RVs, and that both $k$ and $p$ are unknown. Casella and Berger point out that this model has been used to model crimes, where usually neither the total number of crimes $k$ nor the reporting rate $p$ are known.

Again, we have 2 quantities to estimate, so we need 2 moments. Let us first compute the first two theoretical moments of the binomial distribution:
$$\mu_1 = kp$$
$$\mu_2 = kp(1 - p) + k^2 p^2,$$
using a direct calculation based on the law of the lazy statistician, or using the Binomial MGF.

Solving, we obtain:
$$\bar{X} = kp, \quad \frac{1}{n} \sum_{i=1}^n X_i^2 = kp(1 - p) + k^2 p^2$$
which gives
$$\hat{p} = \frac{\bar{X} - \frac{1}{n} \sum_{i=1}^n (X_i - \bar{X})^2}{\bar{X}},$$
$$\hat{k} = \frac{\bar{X}^2}{\bar{X} - \frac{1}{n} \sum_{i=1}^n (X_i - \bar{X})^2}.$$

It is worth pointing out that in this case there are no a-priori “obvious” estimators for the parameters of interest. Further, the method-of-moments estimators in this case can be negative when the sample mean is dominated by the sample variance – a sign of excessive variability – which makes no sense since the parameters are positive.

### Visual Description
Text-only slide.

---
## Page 4
### Content
Another important use of the method-of-moments in statistics is in approximating complex distributions by simpler ones. This is often called moment matching.

**Example 3:** Suppose that $X_1, \dots, X_k$ are each independent $\chi^2_{r_i}$ RVs. To remind you a $\chi^2_t$ distribution is the distribution of the sum of $t$ squared standard normals. The distribution of the sum:
$$S = \sum_{i=1}^k Y_i$$
also has a $\chi^2$ distribution with degrees of freedom $\sum_{i=1}^k r_i$. The distribution of:
$$S_a = \sum_{i=1}^k a_i Y_i,$$
for known constants $a_1, \dots, a_k$ turns out to be much trickier to compute. This type of quantity arises in certain testing problems but also in certain $t$-statistics.

One could try to approximate this distribution by that of a $\chi^2$ distribution. The goal in this case would be to find the value of a parameter $\nu$ such that:
$$\sum_{i=1}^k a_i Y_i \approx \chi^2_\nu$$

### 1.4 Maximum Likelihood Estimators
The most popular technique to derive estimators by maximum likelihood. Suppose that $X_1, \dots, X_n \sim f_\theta$ where $f_\theta$ denotes either the pmf or pdf of the population.

The *likelihood* function is defined by:
$$L(\theta) \equiv L(\theta|X_1, \dots, X_n) = \prod_{i=1}^n f_\theta(X_i).$$

The **maximum likelihood estimator**, or MLE — denoted by $\hat{\theta}$ or $\hat{\theta}_n$ — is the value of the $\theta$ that maximizes $L(\theta)$ (as a function of $\theta$ with $X_1, \dots, X_n$ held fixed).

We write
$$\hat{\theta} = \text{argmax } L(\theta)$$
Keep in mind that $\hat{\theta}$ is a function of the data. Sometimes we will write $\hat{\theta}$ as $\hat{\theta}(X_1, \dots, X_n)$ to emphasize this point.

### Visual Description
Text-only slide.

---
## Page 5
### Content
Intuitively, the MLE is just the value of the parameter which is most likely to have generated the sample and thus is a natural choice as a point estimate. It also has many optimality properties in certain settings: indeed, much of the early theoretical development in statistics revolved around showing the optimality of MLE in various senses.

Note that, unlike the method-of-moments estimator, the MLE estimator has the same range as the parameter.

The typical way to compute the MLE (suppose that we have $k$ unknown parameters) is to either analytically or numerically solve the system of equations:
$$\frac{\partial}{\partial \theta_i} L(\theta) = 0, \quad i = 1, \dots, k.$$

**Example 1:** Suppose $X_1, \dots, X_n \sim N(\theta, 1)$, then the likelihood function is given as:
$$L(\theta) = \prod_{i=1}^n \frac{1}{\sqrt{2\pi}} \exp(-(X_i - \theta)^2/2).$$

A frequently useful simplification is to observe that if $\theta$ maximizes $L(\theta|X_1, \dots, X_n)$ then $\theta$ also maximizes the log-likelihood function:
$$\ell(\theta) \equiv \ell(\theta|X_1, \dots, X_n) = \log L(\theta).$$

Note also that we can throw away any constants not depending on $\theta$ in the likelihood function when we find the MLE. This does not affect the location of the maximizer.

So we have:
$$\hat{\theta} = \text{arg max}_\theta \ell(\theta|X_1, \dots, X_n) = \text{arg min}_\theta \sum_{i=1}^n (X_i - \theta)^2,$$
which yields:
$$\hat{\theta} = \frac{1}{n} \sum_{i=1}^n X_i.$$
We need to further verify that the second derivative of the log-likelihood is negative: in this case the second derivative is $-2n$.

**Example 2:** Suppose that $X_1, \dots, X_n \sim \text{Ber}(p)$, then the log-likelihood is given by
$$\log L(p) \propto \sum_{i=1}^n X_i \log p + (1 - X_i) \log(1 - p) = n\bar{X} \log p + n(1 - \bar{X}) \log(1 - p),$$

### Visual Description
Text-only slide.

---
## Page 6
### Content
which is maximized at
$$\hat{p} = \bar{X}.$$

There are some common difficulties MLEs: the first is computational, how do we find the global maximum of the likelihood function and how do we verify that it is in fact the global maximum? The next two are related to numerical sensitivity: (1) we can often only approximately maximize the likelihood (using a numerical procedure like gradient descent) so we need a well behaved likelihood function (as a function of the parameters); (2) it is also natural to hope that the estimator is a well-behaved function of the data, i.e., we would hope that a slightly different sample would not result in a vastly different MLE.

**Invariance of the MLE** The MLE is invariant to transformations. This means that the MLE of $r(\theta)$ is $r(\hat{\theta})$ for any function $r$. We will not prove this but it is a very useful fact because we are often interested in making inferences about something else than the parameters of the data distribution.

Note that if you know the distribution of $\hat{\theta}$, then you can find the distribution of $r(\hat{\theta})$ using one of the methods we have learned, e.g. the delta method.

### 1.5 Bayes Estimators
The third general method to derive estimators is the Bayes estimator. We treat $\theta$ as a random variable and assign it a distribution $p(\theta)$ called the **prior distribution**.

**\*\*This opens up a bunch of philosophical questions that we will deal with later in the course.\*\***

Now we can use Bayes theorem to get the distribution of $\theta$ given $X_1, \dots, X_n$, which is called the **posterior distribution**:
$$p(\theta|x_1, \dots, x_n) = \frac{p(\theta, x_1, \dots, x_n)}{p(x_1, \dots, x_n)} = \frac{p(x_1, \dots, x_n|\theta)p(\theta)}{\int p(x_1, \dots, x_n|\theta)p(\theta)d\theta}$$
$$= \frac{L(\theta)p(\theta)}{\int L(\theta)p(\theta)d\theta} \propto L(\theta)p(\theta).$$

Finally, we can use the mean of $p(\theta|x_1, \dots, x_n)$ as an estimator:
$$\hat{\theta} = \int \theta p(\theta|X_1, \dots, X_n)d\theta.$$
We call this, the **Bayes estimator**. We could also use the median or mode of the posterior.

### Visual Description
Text-only slide.

---
## Page 7
### Content
**Example 1:** Suppose $X_1, \dots, X_n \sim \text{Ber}(\theta)$. We will first need to define the Beta distribution: $\theta$ has a Beta distribution with parameters $\alpha > 0$ and $\beta > 0$ if its density on $[0, 1]$ is
$$p(\theta) = \frac{\Gamma(\alpha + \beta)}{\Gamma(\alpha)\Gamma(\beta)} \theta^{\alpha-1} (1 - \theta)^{\beta-1} \propto \theta^{\alpha-1} (1 - \theta)^{\beta-1},$$
where $\forall \alpha > 0, \Gamma(\alpha) = \int x^{\alpha-1} e^{-x} dx$. We write $\theta \sim \text{Beta}(\alpha, \beta)$. The mean of the Beta distribution is: $\alpha/(\alpha + \beta)$.

The posterior distribution is
$$p(\theta|X_1, \dots, X_n) \propto L(\theta)p(\theta) \propto \theta^S (1 - \theta)^{n-S} \theta^{\alpha-1} (1 - \theta)^{\beta-1} = \theta^{S+\alpha-1} (1 - \theta)^{n-S+\beta-1},$$
where $S = \sum X_i$. Thus, the posterior distribution is $\text{Beta}(S + \alpha, n - S + \beta)$. We write
$$\theta|X_1, \dots, X_n \sim \text{Beta}(S + \alpha, n - S + \beta).$$
The mean is $(S + \alpha)/(n + \alpha + \beta)$. Thus,
$$\hat{\theta}_n = \frac{S + \alpha}{n + \alpha + \beta}.$$

A common choice is $\alpha = \beta = 1$ (so that the prior for $\theta$ is uniform). In that case:
$$\hat{\theta} = \frac{n\bar{X} + 1}{n + 2} = \frac{n}{n + 2} \bar{X} + \frac{2}{n + 2} \frac{1}{2} = w\bar{X} + (1 - w)\frac{1}{2},$$
which can be viewed as a convex combination of the MLE and the prior mean $1/2$. Note that, when $n$ is large, $\hat{\theta} \approx \bar{X}_n$, which is the mle.

**Exercise:** Suppose that $X_1, \dots, X_n$ drawn from $N(\theta, \sigma^2)$. Assume that $\sigma^2$ is known. Let’s use the prior $\theta \sim N(\mu, \tau^2)$. Prove that the posterior is $N(a, b^2)$ where
$$a = \frac{n\tau^2}{\sigma^2 + n\tau^2} \bar{X}_n + \frac{\sigma^2}{\sigma^2 + n\tau^2} \mu, \quad b^2 = \frac{\sigma^2 \tau^2}{\sigma^2 + n\tau^2}.$$

The Bayes estimator is thus
$$\hat{\mu} = \frac{n\tau^2}{\sigma^2 + n\tau^2} \bar{X}_n + \frac{\sigma^2}{\sigma^2 + n\tau^2} \mu = w \left( \frac{1}{n} \sum_{i=1}^n X_i \right) + (1 - w)\mu$$
where $w = \frac{n\tau^2}{\sigma^2 + n\tau^2}$. When $n$ is large, $w \approx 1$ and $\hat{\mu} \approx \bar{X}_n$.

### Visual Description
Text-only slide.

---
## Page 8
### Content
## 2 Properties of MLEs
We discuss in detail some properties of the MLE. Many of these results are in Chapter 9.4-9.8 of the Wasserman book. In order to get the main ideas across while still conveying some of the proof techniques we will be a bit non-rigorous in various places. In particular, many of the results we state hold under **regularity conditions**, i.e. typically they require that the space of possible parameters is compact, that the true parameter is in the interior of the space of possible parameters and that the likelihood function is smooth. Advanced mathematical statistics textbooks will be more rigorous about these conditions.

Finally, in this lecture we will also ignore computational aspects. The MLE in most cases is not calculated analytically. Rather, we use an algorithm like gradient ascent to try to maximize the likelihood. In some cases (for instance, when the likelihood is concave) we can guarantee that this algorithm will in fact find a parameter sufficiently close to the MLE. In general however computing the MLE can be intractable.

### 2.1 Consistency
The first important property of the MLE is that it is consistent under regularity conditions. As a reminder: consistency means that the MLE converges in probability to the true parameter. The proof is involved so I will not cover it.

The method-of-moments does not typically have such an interpretation so proving general results about its consistency is much more difficult.

### 2.2 Fisher Information
The MLE is often asymptotically Gaussian. In order to express its variance we need to first define what is called the Fisher information matrix.

We first define the **score function**:
$$s_\theta(X) = \frac{\partial \log f_\theta(X)}{\partial \theta};$$
note that $X$ is a single RV with pdf/pmf $f_\theta(X)$.

The score function at $\theta$ has mean zero when $X \sim f_\theta$, i.e.,
$$E_\theta [s_\theta(X)] = 0.$$

### Visual Description
Text-only slide.

---
## Page 9
### Content
**proof:** One can see this by differentiating the expression that $1 = \int_\theta f_\theta(x)dx$, with respect to $\theta$. This yields:
$$0 = \int_\theta \frac{\partial f_\theta(x)}{\partial \theta} dx$$
$$= \int_\theta \frac{\partial \log f_\theta(x)}{\partial \theta} f_\theta(x)dx$$
$$= \mathbb{E}_\theta [s_\theta(X)].$$
**

One can also interpret maximum likelihood as approximately trying to satisfy this condition, i.e., roughly the MLE $\hat{\theta}$ satisfies something like:
$$\frac{1}{n} \sum_{i=1}^n s_{\hat{\theta}}(X_i) = 0,$$
where the sample average is an estimate of the expectation above based on the sample $X_1, \dots, X_n$, which by the WLLN is like trying to solve the equation:
$$\mathbb{E}_{\theta^*} s_\theta(X) = 0.$$
Under identifiability conditions this equation will be uniquely solved at $\theta = \theta^*$. (identifiability = there is a single MLE value – sometimes the likelihood is flat at its maximum so there are many values of $\theta$ that maximize the likelihood.)

The Fisher information is defined as the variance of the score function, i.e.:
$$I(\theta) = \text{Var}_\theta (s_\theta(X)) = \mathbb{E} \left[ s_\theta(X)^2 \right].$$
Under some mild regularity conditions the Fisher information can also be written as:
$$I(\theta) = -\mathbb{E}_\theta \left[ \frac{\partial^2}{\partial \theta^2} \log f_\theta(X) \right].$$

**Proof:** differentiating the score equation we obtain that,
$$\frac{\partial}{\partial \theta} \mathbb{E}_\theta \left[ \frac{\partial}{\partial \theta} \log f_\theta(X) \right] = 0,$$
$$\int_X \left[ \frac{\partial}{\partial \theta} \left( f_\theta(X) \frac{\partial}{\partial \theta} \log f_\theta(X) \right) \right] dX = 0,$$
which after some algebra yields:
$$\mathbb{E}_\theta \left[ \frac{\partial^2}{\partial \theta^2} \log f_\theta(X) \right] + \mathbb{E}_\theta \left[ \frac{\partial}{\partial \theta} \log f_\theta(X) \right]^2 = 0.$$

9
### Visual Description
Text-only slide.

---
## Page 10
### Content
This gives us the desired result.
**

So the Fisher information measures the variance of the score function, but also the Hessian or the curvature of the log-likelihood. One can intuitively imagine that the curvature of the log-likelihood is related to how well we can estimate the unknown parameter. Roughly, if the log-likelihood is very flat then even if our estimate $\hat{\theta}$ is very close in likelihood it need not be the case that $\hat{\theta}$ is close to $\theta^*$. We will try to further formalize this intuition in the next section.

### 2.3 Asymptotic Normality of the MLE
Another important property of the MLE is that under regularity conditions:
$$\sqrt{nI(\theta^*)}(\hat{\theta} - \theta^*)$$
converges in distribution to a standard normal. We have previously seen how one can use this type of convergence in distribution to construct (asymptotic) confidence intervals. A simple argument will also show that this convergence implies that the MSE behaves like:
$$\mathbb{E}(\hat{\theta} - \theta^*)^2 \to \frac{1}{nI(\theta^*)},$$
so we can see that the Fisher information plays a crucial role in determining the quality of the MLE.

We once again have the problem that the confidence intervals above are not constructive since the true parameter $\theta^*$ is not known. Under regularity conditions we also have that:
$$\sqrt{nI(\hat{\theta})}(\hat{\theta} - \theta^*) \to N(0, 1).$$
There are also some cases in which you cannot analytically compute the Fisher information. In these cases often the *observed information* is useful:
$$I_n(\hat{\theta}) = -\frac{1}{n} \sum_{i=1}^n \left[ \frac{\partial^2}{\partial \theta^2} \log f_\theta(X_i)|_{\theta=\hat{\theta}} \right],$$
Under regularity conditions it will also be the case that:
$$\sqrt{nI_n(\hat{\theta})}(\hat{\theta} - \theta^*) \to N(0, 1).$$
Let us try to roughly argue that the asymptotic normality makes sense. Lets define the log-likelihood function as
$$\ell(\theta) = \log L(\theta|X_1, \dots, X_n).$$

10
### Visual Description
Text-only slide.

---
## Page 11
### Content
Since $\hat{\theta}$ maximizes the log-likelihood we have that:
$$0 = \ell'(\hat{\theta}) \approx \ell'(\theta^*) + \ell''(\theta^*)(\hat{\theta} - \theta^*),$$
by a Taylor expansion. Re-arranging this we get that:
$$\sqrt{n}(\hat{\theta} - \theta^*) \approx \frac{\frac{1}{\sqrt{n}}\ell'(\theta^*)}{-\frac{1}{n}\ell''(\theta^*)}.$$
We can analyze the numerator and denominator separately: first the denominator,
$$-\frac{1}{n}\ell''(\theta^*) = -\frac{1}{n} \sum_{i=1}^n \frac{\partial^2}{\partial \theta^2} \log f_\theta(X_i)|_{\theta=\theta^*} \to I(\theta^*).$$
Now the numerator:
$$\frac{1}{\sqrt{n}}\ell'(\theta^*) = \frac{1}{\sqrt{n}} \sum_{i=1}^n \frac{\partial}{\partial \theta} \log f_\theta(X_i)|_{\theta=\theta^*}$$
$$= \frac{1}{\sqrt{n}} \sum_{i=1}^n s_{\theta^*}(X_i).$$
This is a sum of i.i.d. random variables with mean zero, and variance $I(\theta^*)$ so by the CLT we can conclude that:
$$\frac{1}{\sqrt{n}}\ell'(\theta^*) \to N(0, I(\theta^*))$$
in distribution. Putting these two pieces together we obtain the desired result.

At a high-level from a statistical point of view the MLE is a great estimator when appropriate regularity conditions hold: it is invariant (equivariant), it is consistent, and asymptotically normal with a variance that we can compute. Often it will also turn out that the MLE is "optimal" in the sense that amongst all unbiased estimators with an asymptotic Gaussian limit it has the lowest variance (this follows from the Cramer-Rao lower bound). We might delve into this in a future lecture.

Much of modern statistical theory tries to understand cases when: (1) the regularity conditions fail, (2) the asymptotic theory above is not valid (a common example of this is high-dimensional statistics, where the number of parameters greatly exceeds the number of data points) (3) the MLE is intractable to compute (in for instance graphical models and latent variable models).

**Example 1 :** Let $X_1, \dots, X_n \sim \text{Bernoulli}(p)$. Then
$$\ell(p) = S \log p + (n - S) \log(1 - p)$$

11
### Visual Description
Text-only slide.

---
## Page 12
### Content
where $S = \sum_i X_i$. The score function for the sample (i.e. for $n$ observations, not just one) is the first derivative of the log likelihood
$$s = \frac{S}{p} - \frac{n - S}{1 - p}.$$
So the second derivative is: $s' = -S/p^2 - (n - S)/(1 - p)^2$ and we get the Fisher information for the sample by taking minus the expectation
$$I(p) = \frac{np}{p^2} + \frac{n - np}{(1 - p)^2} = \frac{n}{p(1 - p)}.$$
Thus the standard error of the MLE for large $n$ is $\text{se} = \sqrt{p(1 - p)/n}$, which we can approximate with $\widehat{\text{se}} = \sqrt{\hat{p}(1 - \hat{p})/n}$, since $p$ is unknown. An asymptotic confidence interval for $p$ is thus
$$\hat{p} \pm z_{\alpha/2}\widehat{\text{se}} = \hat{p} \pm z_{\alpha/2} \sqrt{\frac{\hat{p}(1 - \hat{p})}{n}}.$$

**Example 2 :** Let $X_1, \dots, X_n \sim \text{Bernoulli}(p)$. We saw that $\text{se} = \sqrt{p(1 - p)/n}$ and an approximate confidence interval is
$$\hat{p} \pm z_{\alpha/2}\widehat{\text{se}} = \hat{p} \pm z_{\alpha/2} \sqrt{\frac{\hat{p}(1 - \hat{p})}{n}}.$$
Suppose we want to estimate $\tau = g(p) = \log(p/(1 - p))$. Then $\hat{\tau} = \log(\hat{p}/(1 - \hat{p}))$, and $g'(p) = 1/(p(1 - p))$. So by the Delta method (see Chapter 5),
$$\text{se}(\hat{\tau}) = |g'(p)|\text{se}(\hat{p}) = \frac{1}{p(1 - p)} \sqrt{\frac{p(1 - p)}{n}} = \frac{1}{\sqrt{np(1 - p)}}$$
and so
$$\widehat{\text{se}}(\hat{\tau}) = \frac{1}{\sqrt{n\hat{p}(1 - \hat{p})}}.$$
An asymptotic $1 - \alpha$ confidence interval for $\tau$ is
$$\hat{\tau} \pm z_{\alpha/2}\widehat{\text{se}}(\hat{\tau}) = \log(\hat{p}/(1 - \hat{p})) \pm \frac{z_{\alpha/2}}{\sqrt{n\hat{p}(1 - \hat{p})}}.$$

**Example 3 :** Let $X_1, \dots, X_n \sim N(\theta, 1)$. Suppose we want to estimate $\tau = e^\theta$. The log-likelihood is $\ell(\theta) = -n(\theta - \bar{X}_n)^2/2$ and the score function is $s = -n(\theta - \bar{X}_n)$ and the Fisher information is $I_n(\theta) = n$. So $\text{se}(\hat{\theta}) = 1/\sqrt{n}$. Now $g'(\theta) = e^\theta$ so $\text{se}(\hat{\tau}) = e^\theta/\sqrt{n}$ and $\widehat{\text{se}}(\hat{\tau}) = e^{\hat{\theta}}/\sqrt{n} = e^{\bar{X}_n}/\sqrt{n}$. The confidence interval is
$$e^{\bar{X}_n} \pm \frac{z_{\alpha/2}e^{\bar{X}_n}}{\sqrt{n}}.$$

12
### Visual Description
Text-only slide.

---
## Page 13
### Content
### 2.4 Multiparameter Problems
Let $\theta = (\theta_1, \dots, \theta_k)$. $L_n(\theta)$ and $\ell_n(\theta)$ are defined as before. The score function $S_n(\theta)$ is now a vector of length $k$ and the $j^{th}$ component is $\partial \ell_n(\theta)/\partial \theta_j$. The Fisher information $I_n(\theta)$ is now a $k \times k$ matrix; it is the variance-covariance matrix of the score. We have the identity:
$$I_n(r, s) = -\mathbb{E}_\theta \left[ \frac{\partial^2 \ell(\theta)}{\partial \theta_r \partial \theta_s} \right].$$
We then have that
$$\sqrt{n}(\hat{\theta} - \theta) \rightsquigarrow N(0, J)$$
where $J = I^{-1}(\theta)$. The standard error of $\hat{\theta}_j$ is $\text{se}(\hat{\theta}_j) = \sqrt{J_{jj}(\theta)/n}$. The confidence interval is $\hat{\theta}_j \pm z_{\alpha/2}\widehat{\text{se}}(\hat{\theta}_j)$.

# 3 Evaluating/Comparing Estimators
We have seen three methods to define estimators: the method of moments, maximum likelihood and Bayes estimator. We have already discussed the MSE and the bias-variance decomposition. Now we discuss *decision theory* more broadly.

### 3.1 Decision Theory
The basic idea is this. We observe data $X_1, \dots, X_n \sim p_\theta$. We construct an estimator $\hat{\theta} = \hat{\theta}(X_1, \dots, X_n)$. To evaluate $\hat{\theta}$ we introduce a loss function $L(\theta, \hat{\theta})$, which measures the loss of estimating $\theta$ with $\hat{\theta}$. Some common loss functions are:

1. **Squared loss:** $L(\theta, \hat{\theta}) = (\theta - \hat{\theta})^2$.
2. **Absolute loss:** $L(\theta, \hat{\theta}) = |\theta - \hat{\theta}|$.
3. **Kullback-Leibler loss:** $L(\theta, \hat{\theta}) = KL(\theta, \hat{\theta}) \equiv \int p_\theta(u) \log \left( \frac{p_\theta(u)}{p_{\hat{\theta}}(u)} \right) du$.

Next, we define the **risk function:**
$$R(\theta, \hat{\theta}) = \mathbb{E}_\theta[L(\theta, \hat{\theta})] = \int L(\theta, \hat{\theta}) p_\theta(x_1) p_\theta(x_2) \dots p_\theta(x_n) dx_1 \dots dx_n,$$
which measures the average loss over all the datasets we could observe from the data distribution. The most commonly used loss function is $L(\theta, \hat{\theta}) = (\theta - \hat{\theta})^2$. The risk function $R = \mathbb{E}[(\theta - \hat{\theta})^2]$ is called the mean squared error, or MSE.

13
### Visual Description
Text-only slide.

---
## Page 14
### Content
We want to choose an estimator $\hat{\theta}$ whose risk function is small. At this point, it is not obvious how to compare risk functions.

**Example 1:** Suppose we have a single observation $X \sim N(\theta, 1)$, and we want an estimate of $\theta$ that minimizes the squared error risk (the MSE). Consider two estimators: $\hat{\theta} = X$ and $\hat{\theta} = 0$. The risk of $X$ is: $\mathbb{E}(X - \theta)^2 = 1$, while the risk of 0 is $\mathbb{E}\theta^2 = \theta^2$. So when $|\theta| < 1$, 0 is a better estimator than the estimator $X$. Neither estimator dominates the other.

**Example 2:** Let us consider the Bernoulli estimation problem: two natural estimators are the MLE:
$$\hat{p}_1 = \frac{1}{n} \sum_{i=1}^n X_i,$$
and the Bayes estimator obtained by using a $\text{Beta}(\alpha, \beta)$ prior is:
$$\hat{p}_2 = \frac{\sum_{i=1}^n X_i + \alpha}{n + \alpha + \beta}.$$
Again, suppose we consider the squared loss:
$$R(p, \hat{p}_1) = \frac{p(1 - p)}{n}.$$
$$R(p, \hat{p}_2) = \text{Var} \left( \frac{\sum_{i=1}^n X_i + \alpha}{n + \alpha + \beta} \right) + \left( \mathbb{E} \frac{\sum_{i=1}^n X_i + \alpha}{n + \alpha + \beta} - p \right)^2.$$
In the second estimator if we choose $\alpha = \beta = \sqrt{n}/4$ we obtain that the risk is constant as a function of $p$, i.e.
$$R(p, \hat{p}_2) = \frac{n}{4(n + \sqrt{n})^2}.$$
We can compare these two estimators' risk functions but once again we see that neither estimator dominates the other everywhere. We need other ways to compare estimators and to find "best" estimators. There are two common ways to do that: **Bayes and Minimax estimators**, and we should weed out **inadmissible** estimators.

### 3.2 Admissibility
We say that an estimator $\hat{\theta}_1$ is inadmissible if there is another estimator $\hat{\theta}_2$ such that,
$$R(\theta, \hat{\theta}_2) \le R(\theta, \hat{\theta}_1),$$
for every $\theta \in \Theta$, and
$$R(\theta, \hat{\theta}_2) < R(\theta, \hat{\theta}_1),$$
for some $\theta \in \Theta$. Otherwise, we say that $\hat{\theta}_1$ is admissible. Being admissible is a weak property. It does not guarantee that the estimator is good.

14
### Visual Description
Text-only slide.

---
## Page 15
### Content
### 3.3 Bayes Estimator
Previously, we called the mean of a posterior distribution, the Bayes estimator. Now we introduce a more general notion of Bayes estimator.

Given a prior $p$ define the **Bayes risk** of $\hat{\theta}$ by
$$R_p(\hat{\theta}) = \int R(\theta, \hat{\theta})p(\theta)d\theta.$$
Note that it is a scalar number, not a function of $\theta$. The **Bayes estimator** or **Bayes rule** is the $\hat{\theta}$ that minimizes $R_p(\hat{\theta})$.

Note that
$$\int R(\theta, \hat{\theta})p(\theta)d\theta = \int \left[ \int L(\theta, \hat{\theta})p(x_1, \dots, x_n|\theta)dx_1 \dots dx_n \right] p(\theta)d\theta$$
$$= \iint L(\theta, \hat{\theta})p(x_1, \dots, x_n|\theta)p(\theta)d\theta dx_1 \dots dx_n$$
$$= \iint L(\theta, \hat{\theta})p(x_1, \dots, x_n, \theta)d\theta dx_1 \dots dx_n$$
$$= \iint L(\theta, \hat{\theta})p(\theta|x_1, \dots, x_n)p(x_1, \dots, x_n)d\theta dx_1 \dots dx_n$$
$$= \int \left[ \int L(\theta, \hat{\theta})p(\theta|x_1, \dots, x_n)d\theta \right] p(x_1, \dots, x_n)dx_1 \dots dx_n$$
$$= \int \mathbb{E}(L(\theta, \hat{\theta}) | x_1, \dots, x_n)p(x_1, \dots, x_n)dx_1 \dots dx_n$$
where
$$\mathbb{E}(L(\theta, \hat{\theta}) | x_1, \dots, x_n) = \int L(\theta, \hat{\theta})p(\theta|x_1, \dots, x_n)d\theta$$
is called the posterior risk. To minimize the Bayes risk, it suffices to choose $\hat{\theta}$ to minimize $\mathbb{E}(L(\theta, \hat{\theta}) | x_1, \dots, x_n)$.

For example, suppose that $L(\theta, \hat{\theta}) = (\theta - \hat{\theta})^2$ is the squared error loss. Then
$$\mathbb{E}(L(\theta, \hat{\theta}) | x_1, \dots, x_n) = \int (\theta - \hat{\theta})^2 p(\theta|x_1, \dots, x_n)d\theta.$$
Take the derivative w.r.t. $\hat{\theta}$ and set it equal to 0. We get
$$\hat{\theta} = \frac{\int \theta p(\theta|x_1, \dots, x_n)d\theta}{\int p(\theta|x_1, \dots, x_n)d\theta} = \mathbb{E}(\theta|x_1, \dots, x_n)$$
which is the posterior mean. Therefore, the posterior mean is the Bayes estimator under squared error loss; by definition, it minimizes the Bayes risk.

15
### Visual Description
Text-only slide.

---
## Page 16
### Content
**Example:** Suppose we have the loss function: $L(\theta, \hat{\theta}) = w(\theta)(\theta - \hat{\theta})^2$, where $w(\theta) \ge 0$ is some positive weighting function. The Bayes rule minimizes the posterior risk, i.e.:
$$\hat{\theta}(X) = \arg \min_{\hat{\theta}} \mathbb{E} \left[ w(\theta)(\theta - \hat{\theta})^2 | X \right].$$
Taking the derivative and setting it to 0, we obtain that the Bayes rule is:
$$\hat{\theta}(X) = \frac{\mathbb{E} [\theta w(\theta)|X]}{\mathbb{E} [w(\theta)|X]},$$
so if we weight the squared loss, we obtain a weighted conditional expectation as the Bayes rule.

**Another way to think about Bayes risk:** An alternative way to think about the Bayes risk is as the average of the loss when we have the generative model: $\theta \sim \pi$ and $X \sim f_\theta$, i.e.,
$$R_\pi(\hat{\theta}) = \mathbb{E} L(\theta, \hat{\theta}(X)),$$
where the expectation is over both $(\theta, X)$.

We end this section by noting that the Bayes estimator depends on the choice of prior distribution and loss function. As a point of comparison, the minimax estimator, introduced below, does not involve the choice of an arbitrary prior so in that sense has some advantages over the Bayes estimator.

### 3.4 Minimax Estimators
The minimax estimator $\hat{\theta}$ is one that minimizes the maximum risk, i.e. the minimax estimator $\hat{\theta}$ satisfies
$$\sup_{\theta \in \Theta} R(\theta, \hat{\theta}) = \inf_{\hat{\theta}'} \sup_{\theta \in \Theta} R(\theta, \hat{\theta}')$$
where the infimum is over all estimators.

**Example 1:** Let us revisit the two Bernoulli estimators from the standpoint of maximum risk and Bayes risk. Suppose we take the uniform prior, then:
$$R_p(\hat{p}_1) = \int \frac{\theta(1 - \theta)}{n} d\theta = \frac{1}{6n},$$
$$R_p(\hat{p}_2) = \frac{n}{4(n + \sqrt{n})^2},$$
so for large $n$ the MLE has smaller Bayes risk since the leading term is $1/6n$ compared to $1/4n$ for the Bayes estimator. On the other hand the Bayes estimator $\hat{p}_2$ always has lower

16
### Visual Description
Text-only slide.

---
## Page 17
### Content
maximum risk (draw the risk curves for the two estimators). This does not mean that $\widehat{\theta}_2$ is minimax; there may be other estimators that have lower maximum risks.

Why look at the max risk as the number to summarize the risk? Because we can find minimax or approximately minimax estimators – see next section. Using the “total risk” (the area under the risk curve) as a one number summary of the risk is for example a reasonable idea, but there is no theory to find the minimum total risk estimator.

### 3.5 Connections

In general, finding exactly minimax estimators is not easy but often we can find approximately minimax (or rate minimax) estimators. There are however, some important connections between Bayes rules, minimax estimators and admissible estimators that are worth knowing.

1. For any estimator the Bayes risk with respect to any prior $\pi$ lower bounds its maximum risk.
2. Suppose that for some prior $\pi$, we have that the corresponding Bayes rule $\widehat{\theta}_\pi$ has the property that:
$$R(\theta, \widehat{\theta}_\pi) \leq R_\pi(\widehat{\theta}_\pi),$$
for every $\theta$. Then $\pi$ is called a **least favorable prior** and $\widehat{\theta}_\pi$ is minimax.
3. A simple consequence of the above is that if for some $\pi$ we have that $R(\theta, \widehat{\theta}_\pi)$ is a constant (as a function of $\theta$) then $\widehat{\theta}_\pi$ is a minimax estimator. In words, a Bayes rule with constant risk is minimax.

**Example:** Recall the Binomial Bayes estimator with $\alpha = \beta = \sqrt{n}/2$. We saw that its MSE is:
$$R(\widehat{p}, p) = \frac{n}{4(n + \sqrt{n})^2}.$$
Since the Bayes estimator has constant risk it is minimax.

**Example:** Suppose we consider Bernoulli estimation with the loss function:
$$L(p, \widehat{p}) = \frac{(p - \widehat{p})^2}{p(1 - p)}.$$
This loss function is sometimes called the Fisher loss.
Let us consider the MLE under this loss. We can see that its risk:
$$R(\widehat{p}, p) = \frac{1}{n},$$

17

### Visual Description
Text-only slide.

---

## Page 18
### Content
which is again constant over the parameter space. Can we conclude that the MLE is minimax? Unfortunately, we do not yet know if it is a Bayes estimator. It turns out that another lengthy calculation reveals that the MLE is actually the Bayes estimator under the uniform prior. So we can conclude that the MLE is minimax with respect to the Fisher loss. This is actually a pretty general phenomenon.

4. Under some conditions (weak regularity conditions, $n$ increases, dimension of $\theta$ is fixed), it can be shown the MLE is approximately minimax.
5. Bayes estimators with respect to certain “nice” priors are admissible. Formally, if the prior $\pi$ is non-zero everywhere over the parameter space and if the risk of the Bayes rule is finite then the Bayes rule is admissible.
6. An estimator with constant risk that is admissible is also minimax.
7. Minimax estimators are not always admissible. This is easy to visualize.

Perhaps the most stunning example of this is due to Charles Stein. This is sometimes called Stein’s phenomenon or Stein’s paradox. We will not go over this in detail but its worth knowing the paradox. The setting is that you observe many different quantities corrupted by Gaussian noise, i.e. we observe:
$$y_i \sim N(\theta_i, 1),$$
for $i \in \{1, \dots, d\}$, and we would like to estimate the vector $\theta$ in MSE. The natural estimator of course is just the observed data, i.e.:
$$\widehat{\theta} = y.$$
It turns out that this estimator is minimax optimal. What Stein showed was that this estimator is inadmissible if $d \geq 3$, in particular he showed that if you make every entry of $y$ slightly smaller the resulting estimator dominates the MLE. The amount by which you make the entries smaller depends on the entire set of observations $y$. This is stunning because the different objects you are measuring are unrelated, yet if you measure more than two objects, you can improve on the obvious estimator by using information you got from *unrelated* measurements.

18

### Visual Description
Text-only slide.
